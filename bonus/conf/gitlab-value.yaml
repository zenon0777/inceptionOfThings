# gitlab-values.yaml
global:
  hosts:
    domain: "nip.io"               # Replace with your actual domain or use nip.io
    externalIP: "10.10.10.10"               # Replace with your cluster IP or external IP if using LoadBalancer
  ingress:
    configureCertmanager: true               # Enables automatic TLS if using cert-manager
  initialRootPassword:
    secret: "JDJhJDEwJG5zZ3Z6b3J"               # Nested 'secret' key for the root password
certmanager:
  install: true                              # Installs cert-manager if needed
certmanager-issuer:
  email: "abderrahmane.daifi@protonmail.com"            # Replace with your email for TLS certificate registration
gitlab-runner:
  install: true                              # Deploys a GitLab Runner for CI/CD jobs


global:
  hosts:
    domain: "nip.io"                      # Replace with your actual domain
    externalIP: "10.43.0.1"                     # Replace with your cluster's external IP if using LoadBalancer
  ingress:
    configureCertmanager: true                     # Enable cert-manager if using it for TLS
  initialRootPassword:
    secret: "mysecurepassword"                     # Ensure you have nested 'secret' for root password
certmanager:
  install: true                                    # Installs cert-manager if not already installed
certmanager-issuer:
  email: "abderrahmane.daifi@protonmail.com"                  # Email for TLS certificate registration
gitlab-runner:
  install: true                                    # Deploys a GitLab Runner for CI/CD jobs
  runners:
    privileged: true                               # Needed if your CI/CD jobs require Docker-in-Docker
